{
  "name": "@blackroad/gateway-worker",
  "version": "1.0.0",
  "description": "BlackRoad OS Cloudflare Workers gateway for long-running and async tasks",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241224.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "wrangler": "^3.0.0"
  }
}
